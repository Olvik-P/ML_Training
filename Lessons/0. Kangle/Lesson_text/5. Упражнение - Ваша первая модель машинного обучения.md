Повторение
До этого момента вы загрузили свои данные и просмотрели их с помощью следующего кода. Запустите эту ячейку, чтобы настроить среду выполнения, с которой завершился предыдущий шаг.


```python
# Код, который вы ранее использовали для загрузки данных
import pandas as pd

# Путь к файлу для чтения
iowa_file_path = '../input/home-data-for-ml-course/train.csv'

home_data = pd.read_csv(iowa_file_path)

# Настройка проверки кода
from learntools.core import binder
binder.bind(globals())
from learntools.machine_learning.ex3 import *

print("Настройка завершена")
```


Упражнения

Шаг 1: Определите целевую переменную
Выберите целевую переменную, которая соответствует цене продажи. Сохраните её в новой переменной с именем `y`. Вам потребуется вывести список столбцов, чтобы найти имя нужного столбца.


```python
# выведите список столбцов в наборе данных, чтобы найти имя целевой переменной
home_data.columns
```


```python
y = ____ # home_data.SalePrice

# Проверьте свой ответ
step_1.check()
```

```python
# Строки ниже покажут вам подсказку или решение.
# step_1.hint() 
# step_1.solution()
```


Шаг 2: Создайте X
Теперь вы создадите DataFrame с именем `X`, содержащий предикторные признаки.

Поскольку вам нужны только некоторые столбцы из исходных данных, сначала создайте список с именами столбцов, которые вы хотите видеть в `X`.

Используйте в списке только следующие столбцы (вы можете скопировать и вставить весь список, чтобы сэкономить время на вводе, хотя вам всё равно потребуется добавить кавычки):

- LotArea
- YearBuilt
- 1stFlrSF
- 2ndFlrSF
- FullBath
- BedroomAbvGr
- TotRmsAbvGrd

После того как вы создали этот список признаков, используйте его для создания DataFrame, который вы будете использовать для обучения модели.


```python
# Создайте список признаков ниже
feature_names = ___ # ['LotArea', 'YearBuilt', '1stFlrSF', '2ndFlrSF', 'FullBath', 'BedroomAbvGr', 'TotRmsAbvGrd']

# Выберите данные, соответствующие признакам из feature_names
X = ____ #  home_data[feature_names]

# Проверьте свой ответ
step_2.check()
```

```python
# step_2.hint()
# step_2.solution()
```


Просмотр данных
Перед построением модели быстро взгляните на `X`, чтобы убедиться, что он выглядит разумно.


```python
# Просмотр данных
# выведите описание или статистику по X
#print(_) # X.describe

# выведите первые несколько строк
#print(_) # X.head()
```


Шаг 3: Укажите и обучите модель
Создайте `DecisionTreeRegressor` и сохраните его в переменной `iowa_model`. Убедитесь, что вы выполнили соответствующий импорт из `sklearn`, чтобы запустить эту команду.

Затем обучите только что созданную модель, используя данные `X` и `y`, которые вы сохранили выше.


```python
# from _ import _
# укажите модель.
# Для воспроизводимости модели задайте числовое значение для random_state при указании модели
iowa_model = ____

# from sklearn.tree import DecisionTreeRegressor
# iowa_model = DecisionTreeRegressor(random_state=1)

# Обучите модель
____ # iowa_model.fit(X, y)

# Проверьте свой ответ
step_3.check()
```

```python
# step_3.hint()
# step_3.solution()
```


Шаг 4: Сделайте прогнозы
Выполните прогнозы с помощью команды `predict` модели, используя `X` в качестве данных. Сохраните результаты в переменной с именем `predictions`.


```python
predictions = ____ # iowa_model.predict(X)
print(predictions)

# Проверьте свой ответ
step_4.check()
```

```python
# step_4.hint()
# step_4.solution()
```


Подумайте о своих результатах
Используйте метод `head`, чтобы сравнить несколько первых прогнозов с фактическими значениями домов (в `y`) для тех же домов. Есть ли что-то удивительное?


```python
# Вы можете написать код в этой ячейке


```


Естественно спросить, насколько точными будут прогнозы модели и как можно их улучшить. Это будет вашим следующим шагом.

Продолжайте
Вы готовы к проверке модели.