# Упражнение - Недообучение и переобучение

## Повторение
Вы построили свою первую модель, и теперь пришло время оптимизировать размер дерева для улучшения прогнозов. Запустите эту ячейку, чтобы настроить среду программирования, на которой остановился предыдущий шаг.

```python
# Код, который вы ранее использовали для загрузки данных
import pandas as pd
from sklearn.metrics import mean_absolute_error
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeRegressor


# Путь к файлу для чтения
iowa_file_path = '../input/home-data-for-ml-course/train.csv'

home_data = pd.read_csv(iowa_file_path)
# Создайте целевой объект и назовите его y
y = home_data.SalePrice
# Создайте X
features = ['LotArea', 'YearBuilt', '1stFlrSF', '2ndFlrSF', 'FullBath', 'BedroomAbvGr', 'TotRmsAbvGrd']
X = home_data[features]

# Разделите на валидационные и обучающие данные
train_X, val_X, train_y, val_y = train_test_split(X, y, random_state=1)

# Определите модель
iowa_model = DecisionTreeRegressor(random_state=1)
# Обучите модель
iowa_model.fit(train_X, train_y)

# Сделайте валидационные прогнозы и вычислите среднюю абсолютную ошибку
val_predictions = iowa_model.predict(val_X)
val_mae = mean_absolute_error(val_predictions, val_y)
print("Валидационная MAE: {:,.0f}".format(val_mae))

# Настройка проверки кода
from learntools.core import binder
binder.bind(globals())
from learntools.machine_learning.ex5 import *
print("\nНастройка завершена")
```

## Упражнения
Вы могли бы написать функцию get_mae самостоятельно. Пока мы её предоставим. Это та же функция, которую вы видели в предыдущем уроке. Просто запустите ячейку ниже.

```python
def get_mae(max_leaf_nodes, train_X, val_X, train_y, val_y):
    model = DecisionTreeRegressor(max_leaf_nodes=max_leaf_nodes, random_state=0)
    model.fit(train_X, train_y)
    preds_val = model.predict(val_X)
    mae = mean_absolute_error(val_y, preds_val)
    return(mae)
```

## Шаг 1: Сравнение различных размеров дерева
Напишите цикл, который перебирает следующие значения max_leaf_nodes из набора возможных значений.

Вызовите функцию get_mae для каждого значения max_leaf_nodes. Сохраните результат таким образом, чтобы можно было выбрать значение max_leaf_nodes, дающее наиболее точную модель на ваших данных.

```python
candidate_max_leaf_nodes = [5, 25, 50, 100, 250, 500]
# Напишите цикл для поиска идеального размера дерева из candidate_max_leaf_nodes
for max_leaf_nodes in candidate_max_leaf_nodes:
    my_mae = get_mae(max_leaf_nodes, train_X, val_X, train_y, val_y)
    print("Максимальное количество листьев: %d \t\t Средняя абсолютная ошибка: %d" % (max_leaf_nodes, my_mae))


# Сохраните лучшее значение max_leaf_nodes (оно будет одним из: 5, 25, 50, 100, 250 или 500)
best_tree_size = ____  # 100

# Проверьте ваш ответ
step_1.check()
```

```python
# Строки ниже покажут подсказку или решение.
# step_1.hint() 
# step_1.solution()
```

## Шаг 2: Обучение модели на всех данных
Вы знаете оптимальный размер дерева. Если вы собираетесь использовать эту модель на практике, вы можете сделать её ещё точнее, используя все данные и сохранив этот размер дерева. То есть теперь, когда вы приняли все решения по моделированию, вам не нужно оставлять валидационные данные.

```python
# Заполните аргумент для оптимального размера и раскомментируйте
final_model = DecisionTreeRegressor( max_leaf_nodes=best_tree_size, random_state=1)

# обучите окончательную модель и раскомментируйте следующие две строки
final_model.fit(X, y)

# Проверьте ваш ответ
step_2.check()
```

```python
# step_2.hint()
# step_2.solution()
```

Вы настроили эту модель и улучшили результаты. Но мы всё ещё используем модели дерева решений, которые по современным стандартам машинного обучения не очень сложны. На следующем шаге вы научитесь использовать случайные леса для дальнейшего улучшения моделей.

## Продолжаем
Вы готовы к случайным лесам.