**Повторение:**
Вы построили модель. В этом упражнении вы проверите, насколько хороша ваша модель.

Запустите ячейку ниже, чтобы настроить среду программирования, с которой закончилось предыдущее упражнение.

```python
# Код, который вы ранее использовали для загрузки данных
import pandas as pd
from sklearn.tree import DecisionTreeRegressor

# Путь к файлу для чтения
iowa_file_path = '../input/home-data-for-ml-course/train.csv'

home_data = pd.read_csv(iowa_file_path)
y = home_data.SalePrice
feature_columns = ['LotArea', 'YearBuilt', '1stFlrSF', '2ndFlrSF', 'FullBath', 'BedroomAbvGr', 'TotRmsAbvGrd']
X = home_data[feature_columns]

# Определение модели
iowa_model = DecisionTreeRegressor()
# Обучение модели
iowa_model.fit(X, y)

print("Первые предсказания на обучающих данных:", iowa_model.predict(X.head()))
print("Фактические значения целевой переменной для этих домов:", y.head().tolist())

# Настройка проверки кода
from learntools.core import binder
binder.bind(globals())
from learntools.machine_learning.ex4 import *
print("Настройка завершена")
```

### Упражнения

**Шаг 1**: Разделите ваши данные
Используйте функцию train_test_split для разделения ваших данных.

Укажите аргумент random_state=1, чтобы проверочные функции знали, чего ожидать при проверке вашего кода.

Напомним, ваши признаки загружены в DataFrame X, а целевая переменная — в y.

```python
# Импортируйте функцию train_test_split и раскомментируйте
# from _ import _  # from sklearn.model_selection import train_test_split

# заполните и раскомментируйте
train_X, val_X, train_y, val_y = ____  # train_test_split(X, y, random_state = 1) 

# Проверьте ваш ответ
step_1.check()
```

```python
# Строки ниже покажут вам подсказку или решение.
# step_1.hint() 
# step_1.solution()
```

**Шаг 2**: Определите и обучите модель
Создайте модель DecisionTreeRegressor и обучите её на соответствующих данных. Снова установите random_state=1 при создании модели.

```python
# Вы уже импортировали DecisionTreeRegressor в предыдущем упражнении
# и этот код был скопирован в код настройки выше. Поэтому не нужно
# импортировать его снова

# Определите модель
iowa_model = ____  # DecisionTreeRegressor(random_state=1) 

# Обучите iowa_model на обучающих данных.
____  # iowa_model.fit(train_X, train_y)

# Проверьте ваш ответ
step_2.check()
```

```python
# step_2.hint()
# step_2.solution()
```

**Шаг 3**: Сделайте предсказания на валидационных данных

```python
# Предскажите для всех валидационных наблюдений
val_predictions = ____  # iowa_model.predict(val_X)

# Проверьте ваш ответ
step_3.check()
```

```python
# step_3.hint()
# step_3.solution()
```

Проверьте ваши предсказания и фактические значения из валидационных данных.

```python
# выведите несколько первых валидационных предсказаний
print(val_predictions[ :5])
# выведите несколько первых фактических цен из валидационных данных
print(val_y.head())
```

Что вы заметили, что отличается от того, что вы видели с предсказаниями на обучающих данных (которые выводятся после верхней ячейки кода на этой странице).

Помните ли вы, почему валидационные предсказания отличаются от предсказаний на обучающих данных? Это важная идея из прошлого урока.

**Шаг 4**: Вычислите среднюю абсолютную ошибку на валидационных данных

```python
from sklearn.metrics import mean_absolute_error
val_mae = ____  # mean_absolute_error(val_y, val_predictions)

# раскомментируйте следующую строку, чтобы увидеть validation_mae
print(val_mae)

# Проверьте ваш ответ
step_4.check()
```

```python
# step_4.hint()
# step_4.solution()
```

Хороша ли эта MAE? Не существует общего правила, какие значения считаются хорошими для всех приложений. Но вы увидите, как использовать (и улучшить) это число в следующем шаге.

Продолжайте
Вы готовы к теме "Недообучение и переобучение".