# Введение

Запустите следующую ячейку, чтобы загрузить данные и некоторые вспомогательные функции.

```python
import pandas as pd

reviews = pd.read_csv("../input/wine-reviews/winemag-data-130k-v2.csv", index_col=0)

from learntools.core import binder; binder.bind(globals())
from learntools.pandas.data_types_and_missing_data import *
print("Готово.")
```

# Упражнения

## 1.

Каков тип данных столбца `points` в наборе данных?

```python
# Ваш код здесь
dtype = ____

# Проверка ответа
q1.check()
```

```python
#q1.hint()
#q1.solution()
```

## 2.

Создайте Series из записей столбца `points`, преобразовав записи в строки. Подсказка: строки в Python обозначаются как `str`.

```python
point_strings = ____

# Проверка ответа
q2.check()
```

```python
#q2.hint()
#q2.solution()
```

## 3.

Иногда столбец `price` содержит пропущенные значения. Сколько обзоров в наборе данных не имеют цены?

```python
n_missing_prices = len(reviews[pd.isnull(reviews.price)])

# Проверка ответа
q3.check()
```

```python
#q3.hint()
#q3.solution()
```

## 4.

Какие регионы производят больше всего вин? Создайте Series, подсчитывающую количество вхождений каждого значения в поле `region_1`. В этом поле часто отсутствуют данные, поэтому замените пропущенные значения на `Unknown`. Отсортируйте по убыванию. Результат должен выглядеть примерно так:

```
Unknown                    21247
Napa Valley                 4480
                           ...  
Bardolino Superiore            1
Primitivo del Tarantino        1
Name: region_1, Length: 1230, dtype: int64
```

```python
reviews_per_region = reviews.region_1.fillna("Unknown").value_counts().sort_values(ascending=False)
# Проверка ответа
q4.check()
```

```python
#q4.hint()
#q4.solution()
```

# Продолжаем

Переходите к переименованию и объединению.