# Упражнение: Группировка и сортировка

## Введение

В этих упражнениях мы применим групповой анализ к нашему набору данных.

Запустите ячейку кода ниже, чтобы загрузить данные перед выполнением упражнений.

```python
import pandas as pd

reviews = pd.read_csv("../input/wine-reviews/winemag-data-130k-v2.csv", index_col=0)
#pd.set_option("display.max_rows", 5)

from learntools.core import binder; binder.bind(globals())
from learntools.pandas.grouping_and_sorting import *
print("Setup complete.")
```

## Упражнения

### 1.

Кто самые популярные рецензенты вин в наборе данных? Создайте Series, в которой индексом будет категория `taster_twitter_handle` из набора данных, а значениями — количество рецензий, написанных каждым автором.

```python
# Ваш код здесь
reviews_written = ____

# Проверка ответа
q1.check()
```

```python
#q1.hint()
#q1.solution()
```

### 2.

Какое лучшее вино я могу купить за определённую сумму? Создайте Series, в которой индексом будут цены на вино, а значениями — максимальное количество баллов, присвоенных вину по такой цене в рецензии. Отсортируйте значения по цене по возрастанию (так, чтобы 4.0 доллара было вверху, а 3300.0 долларов — внизу).

```python
best_rating_per_price = ____

# Проверка ответа
q2.check()
```

```python
#q2.hint()
#q2.solution()
```

### 3.

Каковы минимальная и максимальная цены для каждого сорта вина? Создайте DataFrame, в котором индексом будет категория `variety` из набора данных, а значениями — минимальная и максимальная цены для каждого сорта.

```python
price_extremes = ____

# Проверка ответа
q3.check()
```

```python
#q3.hint()
#q3.solution()
```

### 4.

Какие сорта вин самые дорогие? Создайте переменную `sorted_varieties`, содержащую копию DataFrame из предыдущего вопроса, где сорта отсортированы по убыванию сначала по минимальной цене, затем по максимальной (для разрешения связей).

```python
sorted_varieties = ____

# Проверка ответа
q4.check()
```

```python
#q4.hint()
#q4.solution()
```

### 5.

Создайте Series, в которой индексом будут рецензенты, а значениями — средняя оценка, выставленная каждым рецензентом. Подсказка: вам понадобятся столбцы `taster_name` и `points`.

```python
reviewer_mean_ratings = ____

# Проверка ответа
q5.check()
```

```python
#q5.hint()
#q5.solution()
```

Существуют ли значительные различия в средних оценках, присвоенных разными рецензентами? Запустите ячейку ниже, чтобы использовать метод `describe()` и увидеть сводку диапазона значений.

```python
reviewer_mean_ratings.describe()
```

### 6.

Какие комбинации стран и сортов встречаются чаще всего? Создайте Series, в которой индексом будет MultiIndex из пар {страна, сорт}. Например, сорт «Пино нуар», произведённый в США, должен соответствовать паре `{"US", "Pinot Noir"}`. Отсортируйте значения в Series по убыванию количества вин.

```python
country_variety_counts = ____

# Проверка ответа
q6.check()
```

```python
#q6.hint()
#q6.solution()
```

## Продолжаем обучение

Переходите к теме «Типы данных и пропущенные значения».