Введение

В этом наборе упражнений мы будем работать с набором данных Wine Reviews (Обзоры вин).

Запустите следующую ячейку, чтобы загрузить данные и некоторые вспомогательные функции (включая код для проверки ваших ответов).

```python
import pandas as pd

reviews = pd.read_csv("../input/wine-reviews/winemag-data-130k-v2.csv", index_col=0)
pd.set_option("display.max_rows", 5)

from learntools.core import binder; binder.bind(globals())
from learntools.pandas.indexing_selecting_and_assigning import *
print("Setup complete.")
```

Взгляните на обзор ваших данных, выполнив следующую строку.

```python
reviews.head()
```

Упражнения

### 1.

Выберите столбец `description` из `reviews` и присвойте результат переменной `desc`.

```python
# Ваш код здесь
desc = ____

# Проверьте ваш ответ
q1.check()
```

Дополнительный вопрос: объектом какого типа является `desc`? Если вы не уверены, можете проверить, вызвав функцию Python `type()`: `type(desc)`.

```python
#q1.hint()
#q1.solution()
```

### 2.

Выберите первое значение из столбца `description` таблицы `reviews` и присвойте его переменной `first_description`.

```python
first_description = ____

# Проверьте ваш ответ
q2.check()
first_description
```

```python
#q2.hint()
#q2.solution()
```

### 3.

Выберите первую строку данных (первую запись) из `reviews` и присвойте её переменной `first_row`.

```python
first_row = ____

# Проверьте ваш ответ
q3.check()
first_row
```

```python
#q3.hint()
#q3.solution()
```

### 4.

Выберите первые 10 значений из столбца `description` в `reviews` и присвойте результат переменной `first_descriptions`.

Подсказка: отформатируйте вывод в виде pandas Series.

```python
first_descriptions = reviews.description.iloc[:10]

# Проверьте ваш ответ
q4.check()
first_descriptions
```

```python
#q4.hint()
#q4.solution()
```

### 5.

Выберите записи с индексными метками 1, 2, 3, 5 и 8, присвоив результат переменной `sample_reviews`.

Иными словами, сформируйте следующий DataFrame:

```python
sample_reviews = ____

# Проверьте ваш ответ
q5.check()
sample_reviews
```

```python
#q5.hint()
#q5.solution()
```

### 6.

Создайте переменную `df`, содержащую столбцы `country`, `province`, `region_1` и `region_2` записей с индексными метками 0, 1, 10 и 100. Иными словами, сформируйте следующий DataFrame:

```python
df = ____

# Проверьте ваш ответ
q6.check()
df
```

```python
#q6.hint()
#q6.solution()
```

### 7.

Создайте переменную `df`, содержащую столбцы `country` и `variety` первых 100 записей.

Подсказка: вы можете использовать `loc` или `iloc`. При работе над этим вопросом и несколькими следующими учитывайте следующую особенность, описанную в руководстве:

`iloc` использует схему индексации стандартной библиотеки Python, где первый элемент диапазона включается, а последний исключается. `loc`, в свою очередь, индексирует включительно.

Это особенно запутывает, когда индекс DataFrame представляет собой простой числовой список, например 0,...,1000. В этом случае `df.iloc[0:1000]` вернёт 1000 записей, тогда как `df.loc[0:1000]` вернёт 1001! Чтобы получить 1000 элементов с помощью `loc`, вам нужно опуститься на один ниже и запросить `df.loc[0:999]`.

```python
df = reviews.loc[0:99, ['country', 'variety']]

# Проверьте ваш ответ
q7.check()
df
```

```python
#q7.hint()
#q7.solution()
```

### 8.

Создайте DataFrame `italian_wines`, содержащий обзоры вин, произведённых в Италии. Подсказка: чему равно `reviews.country`?

```python
italian_wines = ____

# Проверьте ваш ответ
q8.check()
```

```python
#q8.hint()
#q8.solution()
```

### 9.

Создайте DataFrame `top_oceania_wines`, содержащий все обзоры с оценкой не менее 95 баллов (из 100) для вин из Австралии или Новой Зеландии.

```python
top_oceania_wines = reviews.loc[reviews.country.isin(['Australia', 'New Zealand']) & (reviews.points >= 95)]

# Проверьте ваш ответ
q9.check()
top_oceania_wines
```

```python
#q9.hint()
#q9.solution()
```

Продолжайте

Переходите к изучению сводных функций и отображений (summary functions and maps).