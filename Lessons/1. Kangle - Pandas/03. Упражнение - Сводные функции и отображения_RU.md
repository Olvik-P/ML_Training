Введение
Теперь вы готовы к более глубокому пониманию ваших данных.

Запустите следующую ячейку, чтобы загрузить данные и некоторые вспомогательные функции (включая код для проверки ваших ответов).

```python
import pandas as pd
pd.set_option("display.max_rows", 5)
reviews = pd.read_csv("../input/wine-reviews/winemag-data-130k-v2.csv", index_col=0)

from learntools.core import binder; binder.bind(globals())
from learntools.pandas.summary_functions_and_maps import *
print("Установка завершена.")
```

```python
reviews.head()
```

Упражнения

### 1.
Чему равно медианное значение столбца `points` в DataFrame `reviews`?

```python
median_points = ____

# Проверьте ваш ответ
q1.check()
```

```python
#q1.hint()
#q1.solution()
```

### 2.
Какие страны представлены в наборе данных? (Ваш ответ не должен содержать дубликатов.)

```python
countries = ____

# Проверьте ваш ответ
q2.check()
```

```python
#q2.hint()
#q2.solution()
```

### 3.
Как часто каждая страна встречается в наборе данных? Создайте Series `reviews_per_country`, отображающий соответствие стран количеству обзоров вин из этой страны.

```python
reviews_per_country = ____

# Проверьте ваш ответ
q3.check()
```

```python
#q3.hint()
#q3.solution()
```

### 4.
Создайте переменную `centered_price`, содержащую версию столбца `price` с вычтенным средним значением цены.

(Примечание: это преобразование «центрирования» является распространённым этапом предварительной обработки перед применением различных алгоритмов машинного обучения.)

```python
centered_price = ____

# Проверьте ваш ответ
q4.check()
```

```python
#q4.hint()
#q4.solution()
```

### 5.
Я экономный покупатель вина. Какое вино является «лучшим по соотношению цена-качество»? Создайте переменную `bargain_wine` с названием вина, имеющего наибольшее соотношение баллов к цене в наборе данных.

```python
bargain_wine = reviews.loc[(reviews.points / reviews.price).idxmax(), 'title']

# Проверьте ваш ответ
q5.check()
```

```python
#q5.hint()
#q5.solution()
```

### 6.
Существует лишь ограниченное количество слов, которые можно использовать для описания бутылки вина. Какое слово встречается чаще — «tropical» (тропический) или «fruity» (фруктовый)? Создайте Series `descriptor_counts`, подсчитывающий, сколько раз каждое из этих двух слов встречается в столбце `description` в наборе данных. (Для простоты давайте проигнорируем версии этих слов, написанные с заглавной буквы.)

```python
n_trop = reviews.description.map(lambda desc: "tropical" in desc).sum()
n_fruity = reviews.description.map(lambda desc: "fruity" in desc).sum()
descriptor_counts = pd.Series([n_trop, n_fruity], index=['tropical', 'fruity'])

# Проверьте ваш ответ
q6.check()
```

```python
#q6.hint()
#q6.solution()
```

### 7.
Мы хотели бы разместить эти обзоры вин на нашем веб-сайте, но рейтинговая система от 80 до 100 баллов слишком сложна для понимания — мы хотим перевести её в простые звёздные рейтинги. Оценка 95 или выше соответствует 3 звёздам, оценка не ниже 85, но ниже 95 — 2 звезды. Любая другая оценка — 1 звезда.

Кроме того, Канадская ассоциация виноделов купила много рекламы на сайте, поэтому любые вина из Канады должны автоматически получать 3 звезды, независимо от количества баллов.

Создайте Series `star_ratings` с количеством звёзд, соответствующим каждому обзору в наборе данных.

```python
def stars(row):
    if row.country == 'Canada' or row.points >= 95:
        return 3
    elif row.points >= 85:
        return 2
    else:
        return 1
    
star_ratings = reviews.apply(stars, axis='columns')

# Проверьте ваш ответ
q7.check()
```

```python
#q7.hint()
#q7.solution()
```

Продолжайте
Переходите к группировке и сортировке.