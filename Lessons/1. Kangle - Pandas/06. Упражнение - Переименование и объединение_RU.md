> **Источник материалов:** [Курс Pandas на Kaggle Learn](https://www.kaggle.com/learn/pandas)
> Все материалы для изучения взяты с данного сайта.

# Упражнение — Переименование и объединение

## Введение

Запустите следующую ячейку, чтобы загрузить данные и некоторые вспомогательные функции.

```python
import pandas as pd

reviews = pd.read_csv("../input/wine-reviews/winemag-data-130k-v2.csv", index_col=0)

from learntools.core import binder; binder.bind(globals())
from learntools.pandas.renaming_and_combining import *
print("Готово.")
```

## Упражнения

Просмотрите первые несколько строк данных, запустив ячейку ниже:

```python
reviews.head()
```

### 1.

`region_1` и `region_2` — довольно неинформативные названия для столбцов с данными о местоположении в наборе данных. Создайте копию `reviews`, переименовав эти столбцы в `region` и `locale` соответственно.

```python
# Ваш код здесь
renamed = reviews.rename(columns={'region_1': 'region', 'region_2': 'locale'})

# Проверка ответа
q1.check()
```

```python
#q1.hint()
#q1.solution()
```

### 2.

Установите имя индекса в наборе данных как `wines`.

```python
reindexed = reviews.rename_axis("wines", axis='rows')

# Проверка ответа
q2.check()
```

```python
#q2.hint()
#q2.solution()
```

### 3.

Набор данных «The Things on Reddit» включает ссылки на товары из подборки популярных форумов («субреддитов») на reddit.com. Запустите ячейку ниже, чтобы загрузить DataFrame с товарами, упомянутыми в субреддите `/r/gaming`, и ещё один DataFrame с товарами из субреддита `/r/movies`.

```python
gaming_products = pd.read_csv("../input/things-on-reddit/top-things/top-things/reddits/g/gaming.csv")
gaming_products['subreddit'] = "r/gaming"
movie_products = pd.read_csv("../input/things-on-reddit/top-things/top-things/reddits/m/movies.csv")
movie_products['subreddit'] = "r/movies"
```

Создайте DataFrame из товаров, упомянутых в любом из этих субреддитов.

```python
combined_products = ____

# Проверка ответа
q3.check()
```

```python
#q3.hint()
#q3.solution()
```

### 4.

Набор данных «Powerlifting Database» на Kaggle включает одну CSV-таблицу с данными о соревнованиях по пауэрлифтингу и отдельную — с данными об участниках. Запустите ячейку ниже, чтобы загрузить эти наборы данных в DataFrame:

```python
powerlifting_meets = pd.read_csv("../input/powerlifting-database/meets.csv")
powerlifting_competitors = pd.read_csv("../input/powerlifting-database/openpowerlifting.csv")
```

Обе таблицы содержат ссылки на `MeetID` — уникальный ключ для каждого соревнования в базе данных. Используя его, создайте набор данных, объединяющий обе таблицы в одну.

```python
powerlifting_combined = powerlifting_meets.set_index(['MeetID']).join(powerlifting_competitors.set_index(['MeetID']), lsuffix='_MEETS', rsuffix='_COMP')


# Проверка ответа
q4.check()
```

```python
#q4.hint()
#q4.solution()
```

## Поздравляем!

Вы завершили мини-курс по Pandas. Многие специалисты по данным считают, что уверенное владение Pandas — это самый полезный и практичный навык, поскольку он позволяет быстро продвигаться в любом проекте.

Если вы хотите применить новые навыки к анализу геопространственных данных, рекомендуем ознакомиться с нашим мини-курсом по геопространственному анализу.

Вы также можете использовать свои навыки Pandas, участвуя в соревнованиях на Kaggle или отвечая на интересующие вас вопросы с помощью наборов данных Kaggle.